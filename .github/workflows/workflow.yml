name: CI workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: 8.5.0-jdk17-alpine
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        run: gradle bootJar

